# 自定义流断流重连功能说明

## 功能概述

为自定义流（FLV/M3U8格式）实现了智能断流重连策略，当流连接中断时，系统会在指定的缓冲时间内进行重连尝试，提高录制的稳定性和连续性。

## 主要特性

### 1. 智能断流检测
- 自动识别自定义流URL（包含`.flv`或`.m3u8`的URL）
- 区分关键错误（404、403、410等）和可重连的临时错误
- 对关键错误立即停止，对临时错误启动重连策略

### 2. 缓冲等待机制
- **缓冲时间**：默认60秒的等待期，在此期间系统会持续尝试重连
- **重连间隔**：默认每10秒进行一次重连尝试
- **超时处理**：超过缓冲时间后自动停止任务

### 3. 状态管理
- 重连期间显示"监控中"状态，提示用户正在尝试重连
- 重连成功后恢复"录制中"状态，继续正常录制
- 支持文件追加模式，重连后继续写入同一文件

## 配置参数

### 用户配置文件设置

在`config/user_settings.json`或通过设置界面配置以下参数：

```json
{
  "custom_stream_buffer_time": 60,        // 断流缓冲等待时间（秒）
  "custom_stream_retry_interval": 10      // 重连尝试间隔（秒）
}
```

### 参数说明

| 参数名 | 默认值 | 说明 |
|--------|--------|------|
| `custom_stream_buffer_time` | 60 | 断流后的缓冲等待时间（秒），在此时间内会持续尝试重连 |
| `custom_stream_retry_interval` | 10 | 每次重连尝试之间的间隔时间（秒） |

## 工作流程

### 正常录制流程
1. 检测到自定义流URL
2. 启动专用的自定义流下载逻辑
3. 建立连接并开始录制

### 断流重连流程
1. **断流检测**：连接中断或出现错误
2. **错误分析**：判断是否为可重连的错误
3. **启动缓冲期**：记录断流开始时间
4. **重连尝试**：每隔指定间隔尝试重新连接
5. **状态更新**：显示重连状态，更新UI
6. **结果处理**：
   - 重连成功：恢复录制，重置计时器
   - 超时失败：停止任务，保留已录制文件

## 日志示例

### 正常启动
```
[INFO] 开始下载自定义流，启用断流重连策略: http://example.com/stream.flv
```

### 断流检测
```
[INFO] 自定义流连接结束，启动断流重连策略: 连接正常结束
[INFO] 自定义流断流检测，开始60秒缓冲等待期: 连接正常结束
```

### 重连尝试
```
[INFO] 自定义流断流重连，剩余等待时间: 50.0秒，10秒后重试
[INFO] 自定义流重连尝试 (第1次): http://example.com/stream.flv
```

### 重连成功
```
[INFO] 自定义流重连成功，继续录制: http://example.com/stream.flv
```

### 重连超时
```
[WARNING] 自定义流断流超过60秒，停止录制任务
```

## 使用建议

### 1. 参数调优
- **稳定网络环境**：可以适当减少缓冲时间（30-45秒）和重连间隔（5-8秒）
- **不稳定网络环境**：建议增加缓冲时间（90-120秒）和重连间隔（15-20秒）
- **高价值直播**：建议设置较长的缓冲时间以最大化录制完整性

### 2. 监控建议
- 关注日志中的重连频率，频繁重连可能表示网络或源端问题
- 定期检查录制文件的完整性
- 对于重要直播，建议同时使用多个录制方案

### 3. 故障排除
- **频繁断流**：检查网络连接稳定性，考虑调整重连参数
- **无法重连**：检查流URL是否仍然有效，确认不是源端问题
- **文件损坏**：检查磁盘空间和写入权限

## 技术实现

### 核心组件
- `DirectStreamDownloader`：直接下载器，支持自定义流重连
- `LiveStreamRecorder`：流录制管理器，集成重连策略
- `RecordingManager`：录制任务管理器，处理任务状态

### 关键方法
- `_download_custom_stream()`：自定义流下载主逻辑
- `_handle_custom_stream_disconnect()`：断流处理和重连策略
- `_is_custom_stream_url()`：自定义流URL识别

## 兼容性

- **支持格式**：FLV、M3U8格式的自定义流
- **平台兼容**：Windows、macOS、Linux
- **Python版本**：3.10+
- **依赖库**：httpx、asyncio

## 更新日志

### v1.0.0
- 实现基础的断流重连功能
- 支持可配置的缓冲时间和重连间隔
- 添加智能错误分析和状态管理
- 完善日志记录和用户界面集成