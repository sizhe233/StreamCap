# 解决端口占用问题

## 问题描述
```
ERROR: [Errno 10048] error while attempting to bind on address ('127.0.0.1', 8000): 通常每个套接字地址(协议/网络地址/端口)只允许使用一次。
```

## 解决方案

### 方法1: 查找并终止占用端口的进程

#### 1. 查找占用 8000 端口的进程
```cmd
netstat -ano | findstr :8000
```

#### 2. 使用 PowerShell 查看详细信息
```powershell
Get-NetTCPConnection -LocalPort 8000 | Select-Object LocalAddress,LocalPort,State,OwningProcess
```

#### 3. 根据进程ID终止进程
```cmd
# 假设进程ID是 12345
taskkill /PID 12345 /F
```

### 方法2: 使用 PowerShell 一键解决
```powershell
# 查找并终止占用 8000 端口的进程
$connections = Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue
if ($connections) {
    foreach ($conn in $connections) {
        $processId = $conn.OwningProcess
        $process = Get-Process -Id $processId -ErrorAction SilentlyContinue
        if ($process) {
            Write-Host "终止进程: $($process.ProcessName) (PID: $processId)"
            Stop-Process -Id $processId -Force
        }
    }
    Write-Host "端口 8000 已释放"
} else {
    Write-Host "端口 8000 未被占用"
}
```

### 方法3: 修改 FastAPI 服务器端口

如果不想终止其他进程，可以修改 FastAPI 服务器使用其他端口：

```python
# 在 main.py 中修改端口
fastapi_server = FastAPIServer(app_manager=app, host="127.0.0.1", port=8001)
```

### 方法4: 使用批处理脚本自动处理

创建 `kill_port_8000.bat` 文件：
```batch
@echo off
echo 正在查找占用端口 8000 的进程...
for /f "tokens=5" %%a in ('netstat -ano ^| findstr :8000') do (
    echo 终止进程 PID: %%a
    taskkill /PID %%a /F
)
echo 端口 8000 已释放
pause
```

## 常用端口管理命令

### 查看所有端口占用情况
```cmd
netstat -ano
```

### 查看特定端口
```cmd
netstat -ano | findstr :端口号
```

### 查看进程详细信息
```cmd
tasklist | findstr PID号
```

### 强制终止进程
```cmd
taskkill /PID 进程ID /F
```

## 预防措施

1. **优雅关闭应用**: 确保应用程序正常退出，释放端口
2. **使用不同端口**: 为不同服务分配不同端口
3. **检查后台进程**: 定期检查是否有僵尸进程占用端口

## 注意事项

- 使用 `/F` 参数强制终止进程时要小心，确保不是系统关键进程
- 如果是自己的 Python 程序占用端口，可以先尝试正常关闭
- 在生产环境中建议使用进程管理工具如 PM2 或 systemd